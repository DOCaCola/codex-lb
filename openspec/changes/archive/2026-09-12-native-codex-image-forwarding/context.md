Codex source c4017a87 sends JSON image requests through its active provider and ChatGPT bearer/account headers. Native requests return created/data[].b64_json and optional generation IDs and image metadata. The public Images API remains a separate multipart/streaming compatibility surface. Native image traffic is independent of the conversation model source; it selects a scoped ChatGPT account without requiring a Responses host model. Upstream errors are not evidence that image POSTs can safely be repeated. Unknown usage is not fabricated.
